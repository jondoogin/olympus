# S12 — Contact details and responsive motion

- Owner confirmed the public GitHub repository is intentional. Replaced the placeholder email with `john@duggan.design` and social links with the supplied Instagram, LinkedIn, Behance and Dribbble profiles. Contact now exposes the email at the top; a standalone form remains unpublished because no delivery service was chosen.
- Added scroll-linked image depth and entrance choreography at widths below 1100px across project cards, case-study media, gallery images, Pantheon portraits, People bios and interior essays. Touch-size project cards reveal their view cue and a fine image rule without hover. Motion uses existing `useScrollProgress` and `Reveal` systems; reduced motion disables new movement and retains visible content.
- PR #5 merged at `1b5dce9`. The Vercel preview and production deployments succeeded. Canonical production HTML served the new JS asset, preserved absolute OG/Twitter URLs and returned no `x-robots-tag: noindex`.
- `npm run build` passed. Local and live browser sweeps of ten routes at 1440, 1024, 768 and 390 px found no horizontal overflow, failed loaded images or Vite overlay. Scroll measurements at 390 and 1024 px showed image translation changing. Reduced-motion VELA used a still with no video, and no reveals stayed hidden. Keyboard Enter opened and closed Services rows. The 390px menu and Contact opener were visually inspected.
- Next human review: motion pacing on a physical phone and tablet; decide whether a standalone form is needed and, if so, where submissions should be delivered.
