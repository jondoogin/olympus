# S8 — Remaining project case studies

- Built complete fictional concept stories for NORTHLINE, HELIO and AURA: brief, idea, system, principles, and outcome. Each outcome explicitly avoids claims of real client work or results.
- Generated three new supporting images per project from its existing campaign image as visual direction. Copied them as new masters under `05-selected-work/` and ran `npm run images` for responsive derivatives and manifest keys. Existing masters were not edited.
- Built three silent 9-second H.264 motion studies from the new stills with `scripts/build-remaining-films.sh`. These are editorial montages of stills, not filmed footage. Each file is about 1 MB.
- Reused the VELA case-study layout for all projects, removed the placeholder body, and made the structured story and media required in the `Project` type.
- Browser checks at 1440, 1024, 768 and 390 px found no horizontal overflow on the three new pages. The check caught a clipped NORTHLINE title inside its line-reveal mask; a project-specific font scale fixed it. Mobile galleries were visually reviewed, responsive images loaded, and videos reported no media errors. The VELA route still rendered after the shared layout change.
- `npm run build` passed. Reduced-motion rendering remains a visual-review item.
- Kept `wip/vela-media` at the prior VELA milestone and branched `wip/all-case-studies` for a single reviewable PR containing all four projects.
