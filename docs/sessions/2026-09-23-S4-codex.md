# S4 — Review fixes · 2026-09-23

Fixed the three findings from S3. `ScrollReset` now updates the document title for each route and focuses `<main>` after client-side navigation; the focus move runs after the mobile menu closes and restores its opener. Titles for fictional project pages state that they are concepts. Closed service panels now use `aria-hidden` until expanded.

Verification: `npm run build` passed. In the browser, closed service descriptions were absent from the accessibility tree and appeared when Strategy opened. The mobile menu's Work link led to `/work` with focus on `<main>` and title `Work | OLYMPUS`; the VELA link led to `/work/vela` with focus on `<main>` and title `VELA — Fictional Concept | OLYMPUS`. No browser warnings or errors appeared. Site code committed as `f9203d8`.
