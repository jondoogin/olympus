# S10 — Deployment reconciliation

- PR #3 merged on `main` at `5e3e58b`. Vercel serves `https://olympus-green.vercel.app`; direct `/work/vela` loads and OG/Twitter URLs are absolute there.
- `VITE_SITE_URL` is set to the canonical origin. Vercel Authentication is off for the project, including previews. Revisit protection if a custom domain is attached.
- The current checkout is `wip/project-media-direction` at `d08d42a`. It contains all four project media stories and predates PR #3, so integrate current `main` before proposing a merge.
- Merged `origin/main` into the media branch, preserving `vercel.json` and the absolute meta plugin. The only merge conflict was the stale `STATE.md` text.
- `npm run images` regenerated the manifest successfully, confirming `sharp` works here. `npm run build` passes after the integration and interior work.
- Built route-specific concept content for Services, People, Culture, About and Contact. Removed the duplicate Pantheon from People; kept the real contact form and address pending owner details. Changed the homepage Work and Proof copy so imagined collaborations and unsupplied collaborator locations do not read as facts.
- Browser sweep on home, four case studies and five interior pages at 1440 / 1024 / 768 / 390 px: no overflow, failed loaded images or Vite overlay. Reduced-motion VELA uses its still and omits video and controls. Services and People were also visually inspected in screenshots.
- Canonical production `/` and `/work/vela` returned 200 without `x-robots-tag`; production HTML had no robots noindex tag and retained absolute OG/Twitter URLs. Branch changes remain local pending owner review and publication.
- Asked owner to confirm whether the public GitHub repository should remain public.
