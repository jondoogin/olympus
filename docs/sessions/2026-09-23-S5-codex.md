# S5 — VELA concept case story · 2026-09-23

Built VELA's first written case story in the shared `/work/:slug` template. It now has brief, idea, system and outcome sections, a typographic statement plate, and an explicit fictional-concept label before the main image. All new story copy lives in `src/content/site.ts`; the other three projects retain the existing template until their own stories are written. Updated `docs/ROUTES.md` to show the remaining content and imagery needs.

`npm run build` passed. The development page loaded with no browser errors. Checked layout width and key text bounds at 1440 / 1024 / 768 / 390 px; no horizontal overflow or clipped headings were found. Visually reviewed the desktop page and 390px hero and story section. VELA currently uses its one supplied campaign image; additional project-specific images are still needed. The Vite dev server emitted existing warnings about `?raw` imports from `public/`, although the production build and rendered page worked. Site and route-outline changes committed as `b5aac20`.
