# S3 — claude — 2026-09-23
Branding and voice pass: "antiquity, rendered". It adds two registers on top of the editorial system. The ancient one uses GFS Didot, Greek numerals, the Delphic maxims and Olympiad dating. The screen one uses IBM Plex Mono and shows real viewport and render-time readouts, the "Excavating…" image placeholder, the oracle status and the "Come back, mortal." tab title.
New: lib/antiquity.ts, sections/Maxims.tsx (on /culture), a "formerly" line per god, museum-label figure notes, a new 404 and new footer lines.
ChatGPT was producing assets in parallel, so I kept out of ProjectPage, projects[], the image pipeline and public/.

## Launch prep (same day, follow-up)
Added vercel.json (SPA rewrite) and a vite.config.ts plugin that makes og:image/twitter:image absolute + adds og:url, gated on a VITE_SITE_URL build env var (no-op if unset). No Vercel account/token available in this session, so the actual project-import + domain-connect step is left for the owner. See STATE.md NOW QUEUE item 6.
